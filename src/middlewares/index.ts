import handlePurchaseQuery from "./middleware.handlePurchase";
import handleMaintainanceQuery from "./middleware.handleMaintainance";

const middlewares = {
    handlePurchaseQuery,
    handleMaintainanceQuery
};

export default middlewares;